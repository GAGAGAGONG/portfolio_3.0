import React from "react";

const Footer = () => {
    return (
        <div className="w-full h-20 bg-[var(--color-primary)] flex justify-center items-center">
            <p className="text-[var(--color-text-muted)] font-mono text-sm">Built with React & Tailwind CSS</p>
        </div>
    );
};

export default Footer;